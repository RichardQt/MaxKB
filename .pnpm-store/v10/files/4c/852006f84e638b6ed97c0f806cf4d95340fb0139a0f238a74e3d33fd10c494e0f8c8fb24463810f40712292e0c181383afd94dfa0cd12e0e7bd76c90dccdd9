import { onBeforeMount as F } from "vue";
const e = "--el-color-primary", M = `${e}-light`, f = `${e}-dark`, l = [3, 5, 7, 8, 9], _ = "#FFFFFF", $ = "#000000", r = document.documentElement, o = (n, s, t) => {
  t = Math.max(Math.min(Number(t), 1), 0);
  const u = parseInt(n.substring(1, 3), 16), a = parseInt(n.substring(3, 5), 16), m = parseInt(n.substring(5, 7), 16), i = parseInt(s.substring(1, 3), 16), p = parseInt(s.substring(3, 5), 16), b = parseInt(s.substring(5, 7), 16), g = Math.round(u * (1 - t) + i * t), I = Math.round(a * (1 - t) + p * t), d = Math.round(m * (1 - t) + b * t), y = ("0" + (g || 0).toString(16)).slice(-2), E = ("0" + (I || 0).toString(16)).slice(-2), P = ("0" + (d || 0).toString(16)).slice(-2);
  return "#" + y + E + P;
}, c = (n) => {
  if (!n)
    return;
  r.style.setProperty(e, n), l.forEach((t) => {
    r.style.setProperty(`${M}-${t}`, o(n, _, t * 0.1));
  });
  const s = o(n, $, 0.2);
  r.style.setProperty(`${f}-2`, s);
};
function R(n) {
  return F(() => {
    c(n);
  }), {
    changeTheme: c
  };
}
export {
  R as useElementPlusTheme
};
